name: kunlunxin_tests

on:
  push:
    branches: ["main", "dev-kunlun"]
  pull_request:
    branches: ["main"]
  workflow_dispatch: # Allows manual triggering

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  te_fl_prepare:
    name: prepare-te-fl-kunlunxin
    uses: ./.github/workflows/prepare_te_fl.yml
    with:
      platform: kunlunxin

  run_tests:
    name: run-tests
    needs: te_fl_prepare
    # Package manager and environment settings are read from .github/configs/kunlunxin.yml
    uses: ./.github/workflows/all_tests_common.yml
    with:
      platform: kunlunxin
      run_unit_tests: true
      run_functional_tests: true
      te_fl_prepare: true
      te_fl_cache_key: ${{ needs.te_fl_prepare.outputs.cache_key }}

  all_tests:
    needs: run_tests
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Verify workflow status
        run: |
          if [ "${{ needs.run_tests.result }}" != "success" ]; then
            echo "❌ Tests workflow failed"
            exit 1
          fi
          echo "✅ All tests passed!"
