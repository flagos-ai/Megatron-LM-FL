# CI/CD Artifact Contract

Bind artifacts to full refs and sort workflows, jobs, references, configs, and routes deterministically. Keep extracted facts immutable; store exclusions and accepted dynamic references separately with owner, reason, reviewer, and evidence.

Acceptance requires unique route coverage, parse status for every YAML, shell syntax status, local-reference disposition, backend/config coverage, and review of every failure-masking candidate.
